(function(){const form=document.getElementById('demo-form');const submitBtn=document.getElementById('submit-btn');const submitText=document.getElementById('submit-text');const submitSpinner=document.getElementById('submit-spinner');const errorMessage=document.getElementById('error-message');const successMessage=document.getElementById('success-message');const formContainer=document.getElementById('demo-form-container');const urlParams=new URLSearchParams(window.location.search);if(urlParams.get('success')==='true'){formContainer.style.display='none';successMessage.style.display='block'}
form.addEventListener('submit',function(e){submitBtn.disabled=!0;submitText.textContent='Submitting...';submitSpinner.classList.add('show');errorMessage.classList.remove('show')});if(urlParams.get('error')==='true'){errorMessage.textContent='Something went wrong. Please try again.';errorMessage.classList.add('show')}})()
